name: CI
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  test:
    name: github-pages
    runs-on: ubuntu-latest
    steps:
      - run: echo "REPO_NAME=${{ github.event.repository.name }}" >> $GITHUB_ENV